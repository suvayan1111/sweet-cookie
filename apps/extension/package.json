{
	"name": "sweet-cookie-extension",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "pnpm run clean && tsc -p tsconfig.json && node scripts/copy-static.mjs",
		"clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
		"typecheck": "tsc -p tsconfig.json --noEmit"
	},
	"devDependencies": {
		"@types/chrome": "^0.1.32"
	}
}
